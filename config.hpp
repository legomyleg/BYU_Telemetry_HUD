#pragma once
#include <string>
using std::string;

inline const char* MODEL_PATH = "resources/models/rocket.glb";
inline constexpr int SAMPLE_RATE = 60;
inline constexpr int SCREEN_WIDTH = 1200;
inline constexpr int SCREEN_HEIGHT = 700;
inline constexpr int SCENE_WIDTH = SCREEN_WIDTH / 3;
inline constexpr int SCENE_HEIGHT = SCREEN_HEIGHT / 2;
inline constexpr int SCENE_RENDER_SCALE = 2;